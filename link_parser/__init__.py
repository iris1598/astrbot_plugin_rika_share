"""莉卡解析插件的实现包（链接分享解析）。

本包是插件根目录 ``main.py`` 之外的实现主体，按职责分层：

- ``adapters``  各平台链接解析适配器。每个适配器模块自注册（URL 匹配正则 + 构建函数），
                新增平台只需新增一个文件并在 ``adapters/__init__.py`` 引入。
- ``models``    通用数据类型（解析结果 / 媒体内容 / 路径任务），
                以及 ``models/platforms/<平台>/`` 下各平台接口返回模型。
- ``services``  有状态服务：流式下载、卡片渲染（``card_render``）、网页截图、
                实况照片合成、B站账号（扫码登录 / Cookie 监控）。
- ``output``    解析结果 → AstrBot 消息链的输出构建（合并转发 / 纯文本 / JSON 卡片）。
- ``utils``     无状态工具函数（缓存、媒体、文本格式化、Cookie）。

插件入口（AstrBot 要求 Handler 必须定义在插件模块内）始终是插件根目录的 ``main.py``。
"""

__all__: list[str] = []
